apiVersion: v1
kind: Service
metadata:
  name: hello-world
  labels:
    package-instance: hello-world
  namespace: example
spec:
  selector:
    package-instance: hello-world
  ports:
  - name: hello-world
    port: 20000
    targetPort: 20000
    protocol: TCP

